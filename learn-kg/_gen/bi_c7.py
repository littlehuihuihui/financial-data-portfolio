# -*- coding: utf-8 -*-
"""BI 章节展开：Power BI（原单叶 → 3 节）。"""

CHAPTER = dict(
    id='BI.Power BI', title='Power BI', level='??', parent='BI',
    prev='Tableau', nxt='国产 BI',
    scene='团队用微软体系，需要把 Tableau 的思路迁移过去。',
    why='工具换了，但「模型 → 度量 → 报表」的分析链路是相通的。',
    rows=[('模型与关系', '星型模型与关系方向。', '??'),
          ('DAX 度量', 'CALCULATE 与上下文。', '??'),
          ('报表与发布', '从画布到工作区。', '??')],
    order='模型与关系 → DAX 度量 → 报表与发布',
)

LESSONS = [
 dict(id='BI.Power BI.模型与关系', title='模型与关系',
      scene='把明细表和表头直接连起来，销售额被放大了好几倍。',
      goal='能搭出星型模型、设定关系基数与方向，避免多对多与双向关系的坑。',
      sample='订单事实表 + 日期维表 + 品类维表。',
      what='Power BI 的模型遵循 **星型模型**：一张事实表（多）+ 若干维表（一）。'
           '关系默认「一对多、单向」，这既符合业务粒度，'
           '又能让 DAX 的筛选沿维表 → 事实表正常传播。',
      steps=['导入事实表与维表，确认各自的粒度（一行代表什么）',
             '在「模型视图」中建立关系：维表(一) → 事实表(多)',
             '确保关系方向为单向（维表筛选事实表）',
             '建立日期表并标记为日期表，供时间智能函数使用',
             '用矩阵表验证：维度 × 度量，合计应与事实表独立聚合一致',
             '清理自动生成的多余关系（Power BI 会自动猜）'],
      code="推荐结构（星型）：\n"
           "  DimDate(一) ──< FactSales(多)\n"
           "  DimProduct(一) ──< FactSales(多)\n"
           "  DimRegion(一) ──< FactSales(多)\n\n"
           "关系设置：基数 = 一对多；交叉筛选方向 = 单一\n"
           "标记日期表：表工具 → 标记为日期表\n\n"
           "验证：矩阵行 = DimProduct[Category]，值 = SUM(FactSales[Amount])",
      result='矩阵里各品类销售额之和等于事实表总销售额；'
             '没有出现因关系导致的重复计数。',
      uses=['建立可维护的数据模型', '时间智能（同比 / 累计）', '替代宽表拼接'],
      upstream='上游是「Tableau」；下游是「DAX 度量」。',
      errors=['事实表之间直接多对多|计数虚高|加桥表或先聚合到统一粒度',
              '用双向关系图省事|筛选环路、结果难预测|优先保持单向',
              '不建日期表|时间智能函数不可用|建 DimDate 并标记',
              '留着一堆自动生成的关系|模型不可读|手工清理并命名'],
      deep='Power BI 的性能与正确性 **八成取决于模型**，而不是 DAX 写得多酷。'
           '星型模型 + 单向关系能让绝大多数业务问题变得简单；'
           '而「多对多 + 双向关系」的模型，后面每一个 DAX 都要靠猜。',
      drill='给你两张表搭一个星型模型，并用矩阵验证合计一致。'),

 dict(id='BI.Power BI.DAX 度量', title='DAX 度量',
      scene='写了 `SUM(利润)/SUM(销售额)`，放进行里结果不对。',
      goal='能写基础聚合度量，并用 `CALCULATE` 做条件与同比计算。',
      sample='利润率、同比、占比三个常用度量。',
      what='DAX 的核心是 **计算上下文**（行上下文 / 筛选上下文）。'
           '度量在「筛选上下文」中求值：切片器、行标题都在改变上下文。'
           '`CALCULATE` 是唯一能 **修改筛选上下文** 的函数，因此它是 DAX 的心脏。',
      steps=['先写最朴素的聚合度量：`销售额 = SUM(FactSales[Amount])`',
             '写比率度量：分子分母各自聚合，不要用行级列相除',
             '用 `CALCULATE` 加条件：如只算已支付',
             '用 `CALCULATE` + `DATEADD` 做同比',
             '用 `DIVIDE` 替代 `/`，自动处理除零',
             '把度量放进矩阵，逐层检查数字是否符合口径'],
      code="销售额 = SUM(FactSales[Amount])\n"
           "利润   = SUM(FactSales[Profit])\n\n"
           "利润率 = DIVIDE([利润], [销售额])        -- 自动防除零\n\n"
           "已支付销售额 =\n"
           "CALCULATE([销售额], FactSales[Status] = \"paid\")\n\n"
           "去年同期 =\n"
           "CALCULATE([销售额], DATEADD(DimDate[Date], -1, YEAR))\n\n"
           "同比 = DIVIDE([销售额] - [去年同期], [去年同期])",
      result='利润率在合计行也正确（分子分母各自聚合后再相除）；'
             '同比首年为空；`DIVIDE` 在分母为 0 时返回空而不是报错。',
      uses=['KPI 与比率指标', '条件口径（已支付 / 已退款）', '时间智能对比'],
      upstream='上游是「模型与关系」；下游是「报表与发布」。',
      errors=['用行级列相除当比率|合计行错、粒度不一致|用两个 `SUM` 相除',
              '用 `/` 直接相除|分母为 0 报错|改 `DIVIDE`',
              '在计算列里写聚合|报错或语义错|度量放度量、行级计算放计算列'],
      deep='理解 DAX 的关键是分清 **「度量」与「计算列」**：'
           '度量在查询时按筛选上下文计算，计算列在刷新时按行上下文计算。'
           '把该做度量的逻辑写成计算列，是 Power BI 里最常见的性能与正确性陷阱。',
      drill='写出利润率与同比两个度量，并解释为什么比率不能用行级列相除。'),

 dict(id='BI.Power BI.报表与发布', title='报表与发布',
      scene='报表做好了，同事打不开、数据也不更新。',
      goal='能完成画布排版、切片器配置、发布到工作区并设置刷新与权限。',
      sample='把度量做成「KPI 卡 + 柱状 + 折线」的一页报表。',
      what='Power BI 的交付链路是 **画布 → 工作区 → 应用 / 权限 → 计划刷新**。'
           '报表页负责呈现，工作区负责协作，网关负责连内网数据源。',
      steps=['画布排版：先放 KPI 卡，再放主图，保持对齐与留白',
             '加切片器（日期 / 地区），设置「同步切片器」跨页一致',
             '设置视觉对象交互：编辑交互 → 选择筛选 / 高亮 / 无',
             '发布到工作区，安装并配置本地数据网关（内网源必需）',
             '设置计划刷新，在报表上标注数据更新时间',
             '建应用 / 配权限，按角色控制可见范围（行级安全用 RLS）'],
      code="发布：主页 → 发布 → 选择工作区\n"
           "网关：管理连接 → 添加本地数据网关（连内网库）\n"
           "刷新：数据集 → 设置 → 计划刷新（如每日 6:00）\n"
           "交互：格式 → 编辑交互 → 筛选 / 高亮 / 无\n"
           "行级安全：建模 → 管理角色 → 用 USERPRINCIPALNAME() 过滤",
      result='同事能在工作区 / 应用中打开报表；'
             '次日刷新后数据自动更新，且能看到标注的更新时间。',
      uses=['团队协作报表', '定时自动刷新', '按角色可见的权限控制'],
      upstream='上游是「DAX 度量」；下游是「国产 BI」与实战。',
      errors=['内网数据源不配网关|刷新一直失败|安装本地数据网关',
              '切片器各页不同步|切换页面后筛选丢失|同步切片器',
              '不设刷新|报表数据长期不动|配置计划刷新并标注时间',
              '权限一刀切|不该看的人能看到全部|用 RLS 按角色过滤'],
      deep='从「能做出报表」到「能交付报表」，中间隔着 **刷新与权限** 这两件事。'
           '很多人的报表只在自己电脑上是对的——'
           '一旦要求「每天自动更新、且只给该看的人看」，才是真正的工程化。',
      drill='把一张报表发布到工作区（或写出你的发布方案），'
            '包含刷新频率与权限设计两部分。'),
]
