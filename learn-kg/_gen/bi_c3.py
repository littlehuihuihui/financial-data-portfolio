# -*- coding: utf-8 -*-
"""Tableau 章节展开：筛选与交互（原单叶 → 3 节）。"""

CHAPTER = dict(
    id='BI.Tableau.筛选与交互', title='筛选与交互', level='??', parent='Tableau',
    prev='计算', nxt='仪表板',
    scene='放了一个筛选器，结果把不该影响的图也一起筛掉了。',
    why='筛选的作用范围与交互动作，决定看板是「能探索」还是「一碰就乱」。',
    rows=[('筛选器作用范围', '这一张图，还是这几张图。', '??'),
          ('参数与集', '把「输入」变成分析维度。', '??'),
          ('操作与联动', '点击驱动别的视图。', '??')],
    order='筛选器作用范围 → 参数与集 → 操作与联动',
)

LESSONS = [
 dict(id='BI.Tableau.筛选与交互.筛选器作用范围', title='筛选器作用范围',
      scene='把地区筛选拖进仪表板，所有图都被筛了，包括本该看全国的对比图。',
      goal='能设置筛选器的应用范围与优先级，做到「只影响该影响的图」。',
      sample='一张全国对比图 + 一张明细表，筛选器只应作用于明细表。',
      what='筛选器有三层概念：**作用范围**（作用于哪些工作表）、'
           '**筛选顺序**（维度筛选 / 度量筛选 / 上下文筛选 / 表计算筛选）'
           '和 **应用方式**（全部用此数据源 / 选定工作表）。'
           '搞混顺序会得到「看起来对但算错」的结果。',
      steps=['拖入筛选器，先选「仅此工作表」确认本图逻辑',
             '在仪表板上右键筛选器 → 应用于工作表 → 选定工作表',
             '需要 LOD 不受筛选影响时，把该筛选器设为「上下文」',
             '用两张图做对照：一张被筛、一张不被筛，验证效果',
             '在图上标注筛选器的作用范围，减少误读'],
      code="作用范围：右键筛选器 → 应用于工作表\n"
           "  ├ 此数据源中的所有工作表\n"
           "  ├ 选定工作表  ← 仪表板混排时最常用\n"
           "  └ 仅此工作表\n\n"
           "筛选顺序：维度 → 度量 → 上下文 → 表计算\n"
           "LOD(FIXED) 在维度筛选之前计算；需要它受影响时用「上下文」",
      result='设置「选定工作表」后，全国对比图合计保持不变，'
             '而明细表随筛选变化——两者互不干扰。',
      uses=['仪表板混排（概览 + 明细）', '保持 KPI 卡为全局口径', '行级权限模拟'],
      upstream='上游是「计算」章的 LOD；下游是「操作与联动」。',
      errors=['默认「所有工作表」|误筛 KPI 卡与对比图|显式改为「选定工作表」',
              'LOD 与维度筛选先后搞混|占比分母被筛选改变|理解顺序，必要时用上下文筛选',
              '把度量筛选当维度筛选|行数不符合预期|先确认筛选发生在哪一层'],
      deep='筛选器的本质是 **「在哪一层、对谁生效」**。'
           '这两个问题的答案不同，结果就完全不同。'
           '专业做法是把「筛选顺序」当成 SQL 的 `WHERE / HAVING / 窗口` 来理解——'
           '它们在 BI 里同样按固定顺序执行。',
      drill='做两张图（全国合计 + 地区明细），让地区筛选只作用于明细图，'
            '并用一句话说明你用了哪个选项。'),

 dict(id='BI.Tableau.筛选与交互.参数与集', title='参数与集',
      scene='业务想自己调「Top N」或切换指标，但每次都要找你改工作簿。',
      goal='能用参数与集把分析自由度交给用户，而不改动计算逻辑。',
      sample='Top N 品类、动态指标切换、按阈值圈选客户。',
      what='**参数**是用户可改的常量（如 N = 10、指标选择），'
           '**集**是满足条件的成员集合（如销售额 Top 10 的品类）。'
           '两者配合，能把「写死的分析」变成「可调的分析」。',
      steps=['新建参数：如 `TopN`，数据类型整数，允许值列表',
             '用参数构造计算字段：`RANK(SUM([Sales])) <= [TopN]`',
             '由该字段创建集（In/Out），把「In」拖入视图做高亮',
             '参数支持指标切换：用 CASE 按参数选 `SUM(Sales)` 或 `SUM(Profit)`',
             '把参数控件显示到视图上，交给用户',
             '验证：改参数值时视图成员同步变化'],
      code="参数：\n"
           "  [TopN] = 10   （整数，允许值：5 / 10 / 20 / 50）\n\n"
           "计算字段：\n"
           "  [IsTopN] = RANK(SUM([Sales])) <= [TopN]\n\n"
           "集：由 [IsTopN] 创建「TopN 品类」集，In/Out\n\n"
           "指标切换：\n"
           "  [Metric] = CASE [MetricPick]\n"
           "               WHEN '销售额' THEN SUM([Sales])\n"
           "               ELSE SUM([Profit]) END",
      result='把参数从 10 改成 5，视图里处于「In」的品类立刻变成 5 个，'
             '底部合计同步更新。',
      uses=['自助式探索（Top N / 阈值）', '一张图切换多个指标', '替代大量复制粘贴的工作表'],
      upstream='上游是「筛选器作用范围」；下游是「操作与联动」。',
      errors=['把 N 写死在计算里|用户无法自助|改参数 + 排名计算',
              '参数控件没显示|用户找不到入口|右键参数 → 显示参数控件',
              '参数与集混用但忘了排序依据|TopN 结果不稳定|排名加唯一键次序键'],
      deep='参数与集的价值是 **「把自由度前置」**：'
           '你不是给用户一张固定的图，而是给一个能自己收放的分析工具。'
           '这也减少了「业务提需求 → 你改工作簿」的往返。',
      drill='做一个带 `TopN` 参数的品类排行视图，并说明参数变化时哪些计算会重算。'),

 dict(id='BI.Tableau.筛选与交互.操作与联动', title='操作与联动',
      scene='汇报时想点一下品类就看到对应趋势，却要手动画筛选器。',
      goal='能配置仪表板操作（筛选 / 高亮 / 参数 / URL），实现点击联动。',
      sample='点击品类柱 → 右侧趋势线自动切到该品类。',
      what='**操作（Actions）** 是把「点击」翻译成「别的视图的变化」。'
           '常用四种：筛选、高亮、转到工作表、更改参数。'
           '它是仪表板从「静态拼图」变成「可探索工具」的关键。',
      steps=['先把两张图放进仪表板（源图 + 目标图）',
             '仪表板 → 操作 → 添加操作 → 选择类型（如筛选）',
             '源工作表 = 品类柱图；目标工作表 = 趋势线',
             '选择触发方式：悬停 / 选择 / 菜单',
             '取消勾选「清除选择时显示全部」（按需），实测点击效果',
             '给用户加提示文字，说明「点击查看详情」'],
      code="仪表板 → 操作 → 添加操作\n"
           "  类型：筛选 / 高亮 / 转到工作表 / 更改参数 / 打开 URL\n"
           "  源：品类柱状图\n"
           "  目标：趋势折线图\n"
           "  触发：选择（点击）  运行方式：选定字段\n\n"
           "高亮 vs 筛选：\n"
           "  高亮：保留全貌 + 突出选中项   （对比场景）\n"
           "  筛选：只显示选中项           （钻取场景）",
      result='点击「家具」柱子，右侧趋势线立刻只画家具的月度趋势；'
             '点击空白处后恢复显示全部。',
      uses=['概览 → 明细的钻取', '对比与聚焦', '跨视图联动叙事'],
      upstream='上游是「参数与集」；下游是「仪表板」章。',
      errors=['高亮与筛选用反|看不到整体对比|对比用高亮、钻取用筛选',
              '操作目标选错工作表|点了没反应|核对源/目标工作表',
              '取消选择后不恢复|用户卡在筛选状态|配置「清除选择时显示全部」'],
      deep='操作设计的关键是 **「保留上下文」**：'
           '高亮让读者既看到选中项、又看到它在整体中的位置；'
           '筛选则丢掉上下文换取聚焦。'
           '分析场景多数应该先高亮、再筛选，让读者自己决定要不要钻下去。',
      drill='做一个「点击品类 → 趋势线联动」的仪表板，'
            '并说明你为什么选高亮而不是筛选。'),
]
