# -*- coding: utf-8 -*-
"""BI 既有 11 节课的内容升级：保留原「是什么 / 怎么做 / 用在哪 / 动手」，补齐缺失段落。"""

UP = {}

UP['BI.Tableau.图表制作.柱状图'] = dict(
    scene='要对比各品类销售额，随手选了饼图，看不出谁大谁小。',
    prereq='「图表类型怎么选」',
    result='柱子按销售额降序排列，第一名一眼可见；类别多时横放成条形图。',
    deep='比较大小的正确编码是 **长度**（柱状 / 条形），而不是角度（饼图）。'
         '类别超过 12 个就横放并只保留 Top N，否则读者无法比较。',
    errors=['用饼图做类别比较|无法判读大小|改柱状 / 条形图',
            '不排序直接展示|重点被埋没|按度量降序排序',
            '类别过多颜色花|视觉噪声|只留 Top N + 其他'])

UP['BI.Tableau.图表制作.折线图'] = dict(
    scene='日期被当成离散文本，折线画成了一节一节的断线。',
    prereq='「维度与度量」',
    result='横轴是连续时间轴，走势平滑；缺数据的日期在「显示缺失值」设置下补点或断开。',
    deep='折线图用 **位置** 编码，适合有顺序的轴（时间、序数）。'
         '把无序的类别画成折线会制造出并不存在的「趋势」。',
    errors=['日期字段为离散|折线断裂、间距不均|右键改为连续',
            '用折线画无序类别|产生虚假趋势|改用柱状图',
            '系列过多|线挤成一团|限制对比系列数'])

UP['BI.Tableau.图表制作.饼图'] = dict(
    scene='把二十个品类做成饼图，小扇区挤在一起完全无法读数。',
    prereq='「图表类型怎么选」',
    result='饼图只保留 ≤5 个类别，其余合并为「其他」，标签显示百分比。',
    deep='饼图的容量上限很低：人的角度判断误差远大于长度判断。'
         '它只适合「A 占大头吗」这类粗判断，不适合精确比较。',
    errors=['十几片饼图|无法比较|改条形图或合并为其他',
            '三维饼图|透视失真、读数不准|用平面饼 / 环',
            '同类多图重复颜色|跨图误解|统一色板'])

UP['BI.Tableau.图表制作.环形图'] = dict(
    scene='想让占比图更轻一些，并在中心放一个总额数字。',
    prereq='「饼图」',
    result='环中心显示合计文本，环本身表达占比，整体比实心饼图更透气。',
    deep='环形图的价值在 **中心的那块留白**：它把「总量」和「构成」放进同一个视觉单元，'
         '适合 KPI + 构成的组合表达。',
    errors=['环过细|无法判读占比|控制内外圆比例',
            '中心塞太多信息|重点丢失|只放一个核心数字'])

UP['BI.Tableau.图表制作.双轴组合图'] = dict(
    scene='想在一张图里同时看销售额和订单数，但两者量级差太大。',
    prereq='「折线图」',
    result='柱与线各用一套刻度；需要时关闭「同步轴」，避免量级差导致一条线贴底。',
    deep='双轴是 **两套坐标系的叠加**，读图时必须明确「哪条线对应哪个轴」。'
         '没有清晰图例与单位标注的双轴图，是数据可视化的常见陷阱。',
    errors=['两轴量级差异不处理|一条线贴底看不见|按需关闭同步轴',
            '不标注对应关系|读者误读|图例与单位写清',
            '双轴当两个图用|信息过载|只叠加真正相关的两个指标'])

UP['BI.Tableau.图表制作.热力图'] = dict(
    scene='要看「地区 × 月份」的强弱分布，普通表格看不出规律。',
    prereq='「双轴组合图」',
    result='格子颜色越深表示数值越大，能一眼看出哪几个「地区 × 月份」组合最强。',
    deep='热力图用 **颜色** 编码，牺牲了精确读数换取了「模式识别」——'
         '它适合找异常组合与季节模式，不适合查具体数字（那要用表格）。',
    errors=['色阶对比不足|深浅看不出差别|改顺序 / 发散色阶',
            '格子数量过多|变成一片色块|聚合维度或缩小范围',
            '不上数值标签|无法读数|按需显示数值'])


UP['BI.Tableau.计算.基础计算'] = dict(
    scene='想加一个「毛利率」字段，直接把两列相除却被提示需要聚合。',
    prereq='「数据准备」',
    result='`[Revenue] - [Cost]` 是行级计算；放进视图时会自动包聚合，'
           '合计行与逐行相除的结果一致。',
    deep='Tableau 的计算分 **行级**（明细，作用在每一行）与 **聚合级**'
         '（作用在视图的每个标记上）。'
         '分不清这两者，就会出现「明细对、合计错」。',
    errors=['行级与聚合混写|报错或结果错|明确字段是行级还是聚合级',
            '硬编码分类值|数据一变就漏|用 `IF / CASE` 规范化',
            '把口径写进图内私有字段|口径漂移|抽成命名字段统一引用'])

UP['BI.Tableau.计算.表计算'] = dict(
    scene='想算各渠道占比，写完公式后占比加起来不是 100%。',
    prereq='「基础计算」',
    result='设好「计算依据」后，渠道占比之和为 100%；同比首年为空。',
    deep='表计算是 **对查询结果再做一次计算**，所以它的结果强烈依赖'
         '「计算依据」（表 / 分区 / 单元格）的方向。'
         '方向错，公式对也算错。',
    errors=['忽略计算依据|占比 / 排名错位|先选方向再写公式',
            '同比分母为 0|出现无穷或报错|用 `ZN / IFNULL` 兜底',
            '表计算与筛选顺序不清|结果随筛选跳变|理解筛选发生在表计算之前'])

UP['BI.Tableau.计算.LOD.FIXED'] = dict(
    scene='想看「每个子类别占其所属品类总额的比例」，但筛选一改分母就跟着变。',
    prereq='「表计算」',
    result='`{FIXED [Category]: SUM([Sales])}` 得到稳定的品类总额，'
           '占比不随视图维度变化。',
    deep='`FIXED` 的含义是 **「忽略视图维度，按我声明的维度算」**。'
         '它是最常用的 LOD，几乎专用于「稳定的分母」。'
         '注意它在维度筛选之前求值——这正是它稳定的原因。',
    errors=['以为 FIXED 受维度筛选影响|占比随筛选跳变|理解求值顺序，必要时用上下文筛选',
            '滥用 FIXED|性能下降、逻辑难懂|只在需要固定粒度时使用',
            '写错固定维度|分母粒度不对|先写清「分母应该是什么粒度」'])

UP['BI.Tableau.计算.LOD.INCLUDE'] = dict(
    scene='想在「品类」视图上算出「平均每个子类别的销售额」。',
    prereq='「LOD.FIXED」',
    result='`{INCLUDE [Sub-Category]: SUM([Sales])}` 先算到子类别粒度，'
           '再在视图粒度上取平均。',
    deep='`INCLUDE` 的含义是 **「在视图粒度上再加一层更细的维度」**。'
         '它天然适合「先算细、再算粗」的均值类需求——'
         '这正是普通 `AVG(SUM(...))` 做不到的。',
    errors=['与视图粒度关系不清|结果不符合预期|先写出视图维度再看 INCLUDE',
            '多层 LOD 嵌套|难读难维护|拆成多个命名字段',
            '用 INCLUDE 做占比|分母粒度错|分母用 FIXED'])

UP['BI.Tableau.计算.LOD.EXCLUDE'] = dict(
    scene='想在子类别明细旁边显示「忽略子类别的品类合计」。',
    prereq='「LOD.INCLUDE」',
    result='`{EXCLUDE [Sub-Category]: SUM([Sales])}` 得到品类级合计，'
           '同一品类下的所有子类别显示同一个值。',
    deep='`EXCLUDE` 与 `FIXED` 的差别是 **「是否随视图维度变化」**：'
         '`EXCLUDE` 从当前视图维度里减掉指定维度，'
         '视图维度一变，结果也跟着变；`FIXED` 则完全由声明决定。',
    errors=['与 FIXED 混淆|结果与预期差一个粒度|记住 EXCLUDE 随视图变化',
            '排除维度不在视图里|结果与直接聚合相同、无效果|确认视图维度',
            '用它求稳定分母|分母仍会变|稳定分母用 FIXED'])

