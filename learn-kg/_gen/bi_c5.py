# -*- coding: utf-8 -*-
"""Tableau 章节展开：性能优化（原单叶 → 3 节）。"""

CHAPTER = dict(
    id='BI.Tableau.性能优化', title='性能优化', level='???', parent='Tableau',
    prev='仪表板', nxt='实战案例',
    scene='工作簿越加越慢，打开一次要几十秒，业务开始不用了。',
    why='性能是交付质量的一部分：慢到没人打开，等于没做。',
    rows=[('为什么慢', '先量再猜：数据量、标记数、计算。', '???'),
          ('数据源优化', '抽取、下推与减少列。', '???'),
          ('视图与计算优化', '降标记数、简计算、控粒度。', '???')],
    order='为什么慢 → 数据源优化 → 视图与计算优化',
)

LESSONS = [
 dict(id='BI.Tableau.性能优化.为什么慢', title='为什么慢',
      scene='凭感觉改来改去，改完也不知道是不是真的快了。',
      goal='能按「数据量 → 标记数 → 计算」的顺序定位瓶颈，并记录基线。',
      sample='一个带高基数上色、多层 LOD、实时连接的工作簿。',
      what='Tableau 渲染成本主要来自三块：'
           '**查询成本**（连库 / 扫描多少行）、'
           '**标记数**（视图上有多少数据点）、'
           '**计算成本**（LOD 与表计算的复杂度）。'
           '先量这三项，再决定优化哪一块。',
      steps=['建立基线：记录打开耗时、底部「标记数」、数据源行数',
             '看数据源：实时还是抽取？是否拉了不需要的列与行',
             '看视图：标记数是否超大（>10 万就要警惕）',
             '看计算：是否有嵌套多层 LOD 或行级函数参与',
             '一次只改一个变量，改完重新记录，验证收益'],
      code="记录基线（写在小本本或工作簿说明页）：\n"
           "  打开耗时：____ 秒\n"
           "  标记数  ：____ 个（视图底部状态栏）\n"
           "  数据源  ：Live / Extract，行数 ____\n\n"
           "优化顺序（收益从大到小）：\n"
           "  1. 数据源：抽取 + 下推筛选 + 减少列\n"
           "  2. 标记数：降基数、聚合、分桶\n"
           "  3. 计算：拆 LOD、避免行级函数堆叠",
      result='你能明确说出「瓶颈在标记数（12 万）」这类判断，'
             '而不是「感觉好用点了」。',
      uses=['接手他人工作簿时的体检', '上线前性能评审', '面试讲优化方法论'],
      upstream='上游是「数据准备」；下游是「数据源优化」。',
      errors=['不记基线直接改|无法验证收益|先记录三项数字',
              '同时改多处|归因不清|一次只动一个变量',
              '只盯计算字段|忽略标记数与数据量|按顺序逐层排查'],
      deep='性能优化是 **科学过程**，不是经验玄学：'
           '量基线 → 提假设 → 改一处 → 再量。'
           '跳过基线这一步，后面所有努力都无法证明有效，'
           '也就无法说服别人（包括未来的自己）。',
      drill='给你手边一个工作簿记录三项基线数字，写出你判断的瓶颈在哪一层。'),

 dict(id='BI.Tableau.性能优化.数据源优化', title='数据源优化',
      scene='实时连接查库很慢，抽取文件又几百 MB。',
      goal='能用抽取、下推筛选与减列把数据层成本压下来。',
      sample='同一个数据源：全表实时 vs 筛选后抽取。',
      what='数据层优化的收益通常 **最大**：减少行（下推筛选）、'
           '减少列（只取需要的字段）、改变形态（实时改抽取）。'
           '把这三件事做好，视图层往往不需要怎么改。',
      steps=['把实时连接改为抽取（报表场景默认）',
             '在抽取时用「筛选器」下推：只取分析所需的时间范围与地区',
             '在数据源中隐藏 / 移除不用的列，减小抽取体积',
             '高基数字段考虑预先分桶（如金额分档），减少后续标记数',
             '设置刷新计划，验证抽取体积与刷新耗时'],
      code="数据 → 提取数据 → 筛选器（下推）\n"
           "  ✔ 订单日期 >= 2023-01-01\n"
           "  ✔ Region IN (华东, 华北)\n\n"
           "数据源页面 → 右键字段 → 隐藏（减少列）\n\n"
           "预分桶（降低后续基数）：\n"
           "  [Sales Band] = IF [Sales] >= 1000 THEN '高'\n"
           "                 ELSEIF [Sales] >= 300 THEN '中'\n"
           "                 ELSE '低' END",
      result='抽取文件体积下降、打开变快；'
             '本来要上色的高基数字段变成了 3 个档位，标记数同步下降。',
      uses=['大数据量报表', '刷新窗口紧张的场景', '多人在线看板'],
      errors=['抽取全表|体积大、刷新慢|用筛选器下推',
              '保留所有列|抽取文件虚胖|隐藏 / 移除不用的列',
              '该抽取却用实时|交互卡顿|报表默认抽取'],
      deep='数据源优化的心法是 **「让计算发生在更便宜的地方」**：'
           '在数据库里过滤（下推）比在 BI 里过滤便宜，'
           '在抽取里过滤比实时查询便宜。'
           '大部分性能问题在数据层就能解决大半。',
      drill='给你工作簿的抽取加两个下推筛选，记录抽取体积与打开耗时的变化。'),

 dict(id='BI.Tableau.性能优化.视图与计算优化', title='视图与计算优化',
      scene='数据源已经抽取了，视图还是卡。',
      goal='能通过降标记数、简化计算与调整粒度让视图流畅。',
      sample='高基数上色 vs 分桶上色；嵌套 LOD vs 拆分为两步。',
      what='视图层的瓶颈几乎总能归结为 **标记数太多**：'
           '高基数维度上色、明细级散点、多层表计算都会让标记数爆炸。'
           '计算优化则是减少嵌套与不必要的高阶函数。',
      steps=['看底部标记数，确认它是不是瓶颈',
             '降基数：高基数字段先分桶 / 聚合，再用于颜色或标签',
             '收口视图：加筛选器减少行，或改用聚合视图',
             '简化计算：把嵌套 LOD 拆成两个字段，便于复用与排查',
             '把常用口径抽成统一命名（`m_` 前缀）的计算字段',
             '改完复测标记数与耗时'],
      code="反例：颜色 = [Order ID]        （基数 = 订单数）\n"
           "正例：颜色 = IF [Sales] > 1000 THEN '高' ELSE '低' END\n\n"
           "反例（嵌套 LOD）：\n"
           "  {FIXED [Cat]: AVG({INCLUDE [Sub]: SUM([Sales])})}\n"
           "正例（拆两步）：\n"
           "  [SubTotal] = {INCLUDE [Sub-Category]: SUM([Sales])}\n"
           "  [CatAvg]   = {FIXED [Category]: AVG([SubTotal])}\n\n"
           "命名规范：m_Sales / m_ProfitRatio / m_Share",
      result='把高基数颜色换成 2 档分桶后，标记数大幅下降、交互明显变快；'
             '拆分后的 LOD 结果与嵌套版本一致但更易读。',
      uses=['仪表板卡顿治理', '多人协作的可维护性', '口径统一治理'],
      upstream='上游是「数据源优化」；下游是「实战案例」章。',
      errors=['高基数维度上色|标记数爆炸、渲染卡|先分桶或聚合',
              '多层 LOD 嵌套|难读难改、性能差|拆成多个命名字段',
              '把明细行直接画散点|几十万标记点|加筛选或改聚合'],
      deep='一个可落地的经验法则：**视图上的标记数尽量控制在几万以内**。'
           '超过这个量级，用户看到的不是更多信息，而是更卡的交互——'
           '信息密度早已超过人眼能分辨的上限。',
      drill='把一个视图的标记数降下来（至少一个数量级），'
            '写出你用的三种手段和优化前后的数字。'),
]
