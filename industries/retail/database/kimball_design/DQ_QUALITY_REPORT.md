# 数据质量检查员 · 零售电商数仓全面质量审查

**行业**：零售电商（XX）  
**对象**：`kimball_design/`（目标权威）+ 三行业一致性对照  
**日期**：2026-07-14

---

## 1. 审查报告

| 审查项 | 检查结果 | 问题详情 | 修复建议 / 处理 |
|--------|---------|---------|----------------|
| 表完整性 | ✅ 通过 | BA 覆盖后 ODS12/DIM9/FACT7/DWS8/ADS8 齐全；凑数表已列待删 | 保持；迁移后 DROP sql6 凑数 ODS |
| 字段完整性 | ✅ 通过 | 各事实含维键+度量+审计字段 | — |
| 数据类型 | ✅ 通过 | 金额 DECIMAL(15,2)；日期 DATE；代理键 BIGINT | — |
| NULL值处理 | ⚠️→✅ | DIM 主键 `*_sk` 缺 `DEFAULT -1`（注释有、DDL 无） | **已补** `02_dim.sql` 全部 `*_sk DEFAULT -1`；事实维键本已 -1，度量本已 0 |
| 主键/外键 | ✅ 通过 | 全表有 PK；外键为逻辑关联（`*_sk`），数仓不建物理 FK（避导入失败） | ETL 必须校验维键落在 DIM（含 -1） |
| 业务场景覆盖 | ✅ 通过 | 下单/支付/退货/会员/库存/费用/预算均有 fact_* | 见 BA 报告 |
| 跨行业一致性 | ⚠️→部分对齐 | 互联/制造事实物理名仍为 `dwd_*_wide`；互联金额精度混用 | **已**：跨行业标准文档；`fact_*` 同义视图；互联金额改 15,2 |

---

## 2. 修改方案（已执行）

1. 零售 DIM：所有 `*_sk` 增加 `DEFAULT -1`  
2. 互联网：金额字段统一 `DECIMAL(15,2)`；`cac` 默认 0；新增 `03_fact_aliases.sql`  
3. 制造：新增 `07_fact_aliases.sql`（`fact_production/quality/supply`）  
4. 新增统一规范：`portfolio/docs/dw_cross_industry_quality_standard.md`

---

## 3. 修复后的完整 DDL

- 零售：`kimball_design/01_ods.sql`～`05_ads.sql`（已含本次 DEFAULT）  
- 互联：`01_ddl.sql` + `03_fact_aliases.sql`  
- 制造：`01_ods.sql`～`05_ads.sql` + `07_fact_aliases.sql`

---

## 4. 一致性检查报告

| 规范项 | 零售 | 互联网 | 制造 |
|--------|------|--------|------|
| 分层 ODS→DWD→DWS→ADS | ✅ | ✅ | ✅ |
| 事实消费名 `fact_*` | ✅ 物理 | ✅ 视图 | ✅ 视图 |
| 金额 DECIMAL(15,2) | ✅ | ✅ | ✅ |
| 维键 DEFAULT -1 | ✅ | ⚠️ 业务键为主（部分 sk 已有） | ✅ |
| 业务日期 DATE | ✅ | ✅ 主体 | ✅ |
| 字段数下限 | ✅ | ⚠️ 部分历史薄表已扩，重建后达标 | ✅（已按标准扩） |
| ADS 禁 ODS | ✅ | ✅ | ✅ |

**结论**：三套共用同一质量规范文档；零售为目标样板；互联/制造通过别名视图与金额类型对齐，物理表名可在下次重建时升格为 `fact_*`。
