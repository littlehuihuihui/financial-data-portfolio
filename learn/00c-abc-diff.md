# A/B/C 三层现实 · 可视化对照

> 配合 [`00b-three-realities.md`](00b-three-realities.md)。本页用「同一主题」对照三种实现，降低混淆。

## 主题：开机 → 日活

| 维度 | A 演示灌数 | B 规范 ETL（教材范式） | C 靶场 `learn_lab_dau` |
|------|------------|------------------------|------------------------|
| 库 | `internet_analytics` | （设计目标） | `learn_lab_dau` |
| 代码 | `seed_ott.py` · `seed_raw_logs` 等 | 第 03 章 MySQL 骨架 | `learn/lab/0*.sql` |
| ODS→DWD | **常并行写入**（synthetic） | 过滤→关联维→去重→INSERT | 坏/好两套脚本可切换 |
| 调度 | 手动跑种子 | T+1 依赖链（理论） | 本地命令行一次跑完 |
| 数字 | 万级 mac 演示 | — | 修复后 DAU=3 |
| 看板 | 读 A | 应用 B 的纪律 | **不**直接驱动看板 |

```
        ┌──────────── A 演示灌数 ────────────┐
源概念 ─►│ seed_ott → internet_analytics      │─► 看板 / 实验台
        └────────────────────────────────────┘

        ┌──────────── B 规范形态 ────────────┐
需求   ─►│ ODS→DWD→DWS→ADS（纪律+骨架）       │─► 设计作业 / 答辩
        └────────────────────────────────────┘

        ┌──────────── C 靶场 ────────────────┐
脏数据 ─►│ learn_lab_* 弄坏→DQ→修好→TOKEN     │─► 技能肌肉记忆
        └────────────────────────────────────┘
```

## 主题：点播完播 / 收银漏斗

| | A | B | C |
|--|---|---|---|
| 完播 | seed 写入 `dws_content_series_play_1d.finish_cnt` | 用 `is_finish` 聚合 | `--suite vod` |
| 漏斗 | seed → `dws_trade_cashier_funnel_1d`；ADS `v_funnel` 月聚合 | 步骤枚举+单调性 | `--suite funnel` 日视图 |

## 一句话自检

问自己：我现在看的数字/SQL，属于 A、B 还是 C？说不出字母＝还没分清。
