{"id":"sql-root","title":"SQL","level":"?","content":"### 课前 · 章节导读\n\n- **章节**：SQL\n- **为什么学**：把「SQL」拆成可练习的小节，避免只记目录。\n- **学完能做什么**：按顺序完成下面每一节的「动手」，并能讲清本节边界。\n- **纪律**：使用全平台统一样例（订单 / 用户 / Superstore），不要每节换一套数据。\n\n### 本节地图\n\n| # | 节点 | 难度 | 一句话 |\n|---|---|---|---|\n| 1 | 基础查询 | ? | 章节：基础查询 |\n| 2 | 多表操作 | ?? | 章节：多表操作 |\n| 3 | 聚合分析 | ?? | 章节：聚合分析 |\n| 4 | 数据定义 | ??? | 目标：能用 CREATE TABLE 声明类型、主键和非空，并知道 DDL 是结… |\n| 5 | 性能优化 | ??? | 目标：能用过滤、列裁剪和索引思路判断一条查询为什么慢。 |\n| 6 | 事务与安全 | ??? | 目标：能说明事务的提交/回滚，以及分析账号为什么不能要写权限。 |\n\n### 推荐顺序\n\n```text\n基础查询 → 多表操作 → 聚合分析 → 数据定义 → 性能优化 → 事务与安全\n```\n\n### 怎么学\n\n1. 先看地图，知道有哪些节点。\n2. 按顺序打开，每节做完「动手」再往下。\n3. 用自己的表名替换样例，做一次小样本迁移。\n\n### 验收\n\n| 检查 | 标准 |\n|---|---|\n| 主路径 | 每节示例能重做 |\n| 易错 | 至少能举出 2 个反例 |\n| 口述 | 不看笔记讲清「SQL」解决什么 |\n\n### 下一动\n\n从第 1 个节点开始。本章共 **6** 个直接下级。\n","children":[{"id":"SQL.基础查询","title":"基础查询","level":"?","content":"### 课前 · 章节导读\n\n- **章节**：基础查询\n- **为什么学**：把「基础查询」拆成可练习的小节，避免只记目录。\n- **学完能做什么**：按顺序完成下面每一节的「动手」，并能讲清本节边界。\n- **纪律**：使用全平台统一样例（订单 / 用户 / Superstore），不要每节换一套数据。\n\n### 本节地图\n\n| # | 节点 | 难度 | 一句话 |\n|---|---|---|---|\n| 1 | SELECT | ? | 目标：能写出显式列、别名、表达式与 DISTINCT，并说清结果集是什么。 |\n| 2 | WHERE | ? | 目标：能组合 AND/OR/NOT、区间与 NULL 判断，并知道 WHERE … |\n| 3 | ORDER BY | ? | 目标：能按一列或多列排序，并处理 NULL 与并列名次。 |\n| 4 | LIMIT | ? | 目标：能安全取前 N 行，并知道必须先 ORDER BY。 |\n\n### 推荐顺序\n\n```text\nSELECT → WHERE → ORDER BY → LIMIT\n```\n\n### 怎么学\n\n1. 先看地图，知道有哪些节点。\n2. 按顺序打开，每节做完「动手」再往下。\n3. 用自己的表名替换样例，做一次小样本迁移。\n\n### 验收\n\n| 检查 | 标准 |\n|---|---|\n| 主路径 | 每节示例能重做 |\n| 易错 | 至少能举出 2 个反例 |\n| 口述 | 不看笔记讲清「基础查询」解决什么 |\n\n### 下一动\n\n从第 1 个节点开始。本章共 **4** 个直接下级。\n","children":[{"id":"SQL.基础查询.SELECT","title":"SELECT","level":"?","content":"### 课前\n\n- **目标**：能写出显式列、别名、表达式与 DISTINCT，并说清结果集是什么。\n- **先修**：先看上一节；样例与全平台一致。\n- **样例**：统一样例：users / orders / order_items。\n\n### 是什么\n\nSELECT 决定「返回哪些列」。它不负责过滤（WHERE）也不负责排序（ORDER BY）。`SELECT *` 只适合探查，生产查询应写列名，避免上游加列把下游报表撑爆。\n\n### 怎么做\n\n```sql\nSELECT\n  o.order_id,\n  o.user_id,\n  o.amount AS gmv,\n  o.amount * 0.06 AS tax_est\nFROM orders AS o\nWHERE o.status = 'paid';\n\nSELECT DISTINCT status FROM orders;\n```\n\n### 用在哪\n\n1. 日报只要订单号与金额，不要整行。\n2. 给指标起业务别名（gmv），避免下游猜 amount 含不含税。\n3. 枚举状态用 DISTINCT，先看有哪些取值再写过滤。\n\n### 易错对照\n\n| 错法 | 纠正 |\n|---|---|\n| SELECT * 进生产报表 | 显式列名，并在注释里写口径 |\n| 把过滤写进 SELECT | 过滤放 WHERE；SELECT 只投影 |\n| 别名用了中文空格又没加引号 | 用 AS gmv 这种标识符 |\n\n### 动手\n\n查询已支付订单的 order_id、user_id、amount（别名 gmv）。不要用 SELECT *。\n\n### 验收\n\n- [ ] 能不看笔记复述本节目标\n- [ ] 示例可以自己重做一遍\n- [ ] 能举出一个易错并改对\n- [ ] 能说出本节不该用的场景\n\n### 导航\n\n- 上级：`基础查询`\n- 上一节：（本节起）\n- 下一节：WHERE\n","children":[]},{"id":"SQL.基础查询.WHERE","title":"WHERE","level":"?","content":"### 课前\n\n- **目标**：能组合 AND/OR/NOT、区间与 NULL 判断，并知道 WHERE 在分组前执行。\n- **先修**：先看上一节；样例与全平台一致。\n- **样例**：统一样例：users / orders / order_items。\n\n### 是什么\n\nWHERE 按行过滤。`NULL` 不是空字符串，`= NULL` 永远未知，必须用 `IS NULL`。OR 与 AND 混用时加括号。\n\n### 怎么做\n\n```sql\nSELECT order_id, amount, status\nFROM orders\nWHERE status = 'paid'\n  AND amount >= 100\n  AND created_at >= DATE '2024-01-01'\n  AND amount IS NOT NULL;\n```\n\n### 用在哪\n\n1. 看板只看已支付且金额有效的订单。\n2. 排除测试城市或空城市用户。\n3. 先过滤再 JOIN，减少参与关联的行。\n\n### 易错对照\n\n| 错法 | 纠正 |\n|---|---|\n| 写成 amount = NULL | 改为 amount IS NULL / IS NOT NULL |\n| AND/OR 没加括号 | 先写括号再填条件 |\n| 在 WHERE 里用聚合函数 | 聚合后过滤用 HAVING |\n\n### 动手\n\n找出 2024-01 之后、状态为 paid、金额不为空且 ≥ 50 的订单。\n\n### 验收\n\n- [ ] 能不看笔记复述本节目标\n- [ ] 示例可以自己重做一遍\n- [ ] 能举出一个易错并改对\n- [ ] 能说出本节不该用的场景\n\n### 导航\n\n- 上级：`基础查询`\n- 上一节：SELECT\n- 下一节：ORDER BY\n","children":[]},{"id":"SQL.基础查询.ORDER BY","title":"ORDER BY","level":"?","content":"### 课前\n\n- **目标**：能按一列或多列排序，并处理 NULL 与并列名次。\n- **先修**：先看上一节；样例与全平台一致。\n- **样例**：统一样例：users / orders / order_items。\n\n### 是什么\n\nORDER BY 在 SELECT 之后执行，可以用别名。不写 ORDER BY 时，数据库不保证行顺序，「最新一条」没有定义。\n\n### 怎么做\n\n```sql\nSELECT order_id, amount, created_at\nFROM orders\nWHERE status = 'paid'\nORDER BY created_at DESC, order_id ASC;\n```\n\n### 用在哪\n\n1. 订单时间线从新到旧。\n2. 金额相同再按订单号稳定排序，方便对账。\n3. 给 LIMIT 提供确定的「前 N」。\n\n### 易错对照\n\n| 错法 | 纠正 |\n|---|---|\n| 只 ORDER BY 一列，并列行每次顺序不同 | 再加唯一键如 order_id |\n| 以为 SELECT 里的顺序就是结果顺序 | 展示顺序只看 ORDER BY |\n| 对未过滤的大表全表排序 | 先 WHERE 再排序 |\n\n### 动手\n\n按支付时间倒序列出 paid 订单，时间相同则按 order_id 升序。\n\n### 验收\n\n- [ ] 能不看笔记复述本节目标\n- [ ] 示例可以自己重做一遍\n- [ ] 能举出一个易错并改对\n- [ ] 能说出本节不该用的场景\n\n### 导航\n\n- 上级：`基础查询`\n- 上一节：WHERE\n- 下一节：LIMIT\n","children":[]},{"id":"SQL.基础查询.LIMIT","title":"LIMIT","level":"?","content":"### 课前\n\n- **目标**：能安全取前 N 行，并知道必须先 ORDER BY。\n- **先修**：先看上一节；样例与全平台一致。\n- **样例**：统一样例：users / orders / order_items。\n\n### 是什么\n\nLIMIT 只截断已经排好的结果。没有 ORDER BY 的 LIMIT 取出的是任意 N 行。分页用 LIMIT + OFFSET 时，大偏移会变慢，深分页应改用「上次最大 id」。\n\n### 怎么做\n\n```sql\nSELECT order_id, amount, created_at\nFROM orders\nWHERE status = 'paid'\nORDER BY created_at DESC, order_id DESC\nLIMIT 5;\n```\n\n### 用在哪\n\n1. 首页只展示最近 5 笔支付。\n2. 抽样探查表结构，不要一次拉全表。\n3. 和 ORDER BY 一起定义「Top N 客户」。\n\n### 易错对照\n\n| 错法 | 纠正 |\n|---|---|\n| LIMIT 5 却没有 ORDER BY | 先定义排序键再截断 |\n| OFFSET 很大当成分页方案 | 改用 keyset：WHERE id < :last_id |\n| LIMIT 写在 WHERE 前面 | 子句顺序：WHERE → ORDER BY → LIMIT |\n\n### 动手\n\n取最近 3 笔已支付订单（时间倒序，并列用 order_id）。\n\n### 验收\n\n- [ ] 能不看笔记复述本节目标\n- [ ] 示例可以自己重做一遍\n- [ ] 能举出一个易错并改对\n- [ ] 能说出本节不该用的场景\n\n### 导航\n\n- 上级：`基础查询`\n- 上一节：ORDER BY\n- 下一节：（本节止）\n","children":[]}],"lessonParent":true},{"id":"SQL.多表操作","title":"多表操作","level":"??","content":"### 课前 · 章节导读\n\n- **章节**：多表操作\n- **为什么学**：把「多表操作」拆成可练习的小节，避免只记目录。\n- **学完能做什么**：按顺序完成下面每一节的「动手」，并能讲清本节边界。\n- **纪律**：使用全平台统一样例（订单 / 用户 / Superstore），不要每节换一套数据。\n\n### 本节地图\n\n| # | 节点 | 难度 | 一句话 |\n|---|---|---|---|\n| 1 | JOIN | ?? | 目标：能按粒度选择 INNER / LEFT JOIN，并避免一对多把金额加爆。 |\n| 2 | 子查询 | ?? | 目标：能区分标量子查询、IN 列表与派生表，并知道何时改写成 JOIN。 |\n| 3 | UNION | ?? | 目标：能用 UNION / UNION ALL 叠结果，并说清何时不能去重。 |\n\n### 推荐顺序\n\n```text\nJOIN → 子查询 → UNION\n```\n\n### 怎么学\n\n1. 先看地图，知道有哪些节点。\n2. 按顺序打开，每节做完「动手」再往下。\n3. 用自己的表名替换样例，做一次小样本迁移。\n\n### 验收\n\n| 检查 | 标准 |\n|---|---|\n| 主路径 | 每节示例能重做 |\n| 易错 | 至少能举出 2 个反例 |\n| 口述 | 不看笔记讲清「多表操作」解决什么 |\n\n### 下一动\n\n从第 1 个节点开始。本章共 **3** 个直接下级。\n","children":[{"id":"SQL.多表操作.JOIN","title":"JOIN","level":"??","content":"### 课前\n\n- **目标**：能按粒度选择 INNER / LEFT JOIN，并避免一对多把金额加爆。\n- **先修**：先看上一节；样例与全平台一致。\n- **样例**：统一样例：users / orders / order_items。\n\n### 是什么\n\nJOIN 把两张表按键对齐。INNER 丢掉对不上的行；LEFT 保留左表。订单对明细是一对多，先按订单汇总明细再 JOIN 订单，否则 SUM(orders.amount) 会被明细行放大。\n\n### 怎么做\n\n```sql\nSELECT o.order_id, o.amount, u.user_name, u.city\nFROM orders AS o\nLEFT JOIN users AS u ON u.user_id = o.user_id\nWHERE o.status = 'paid';\n\n-- 明细先聚合再回订单\nSELECT o.order_id, o.amount, s.item_amt\nFROM orders AS o\nLEFT JOIN (\n  SELECT order_id, SUM(price * qty) AS item_amt\n  FROM order_items\n  GROUP BY order_id\n) AS s ON s.order_id = o.order_id;\n```\n\n### 用在哪\n\n1. 给订单补用户城市。\n2. 找出没有订单的用户（LEFT JOIN + 右表键 IS NULL）。\n3. 对账：订单金额 vs 明细合计。\n\n### 易错对照\n\n| 错法 | 纠正 |\n|---|---|\n| 一对多之后直接 SUM(订单金额) | 先聚合到订单粒度再加总 |\n| 只用 INNER，把没匹配的用户悄悄丢掉 | 需要保留时用 LEFT，并检查 NULL |\n| JOIN 条件写成 WHERE 里的过滤，把 LEFT 变成 INNER | 对右表的过滤放 ON，或明确接受 INNER |\n\n### 动手\n\n列出每个用户的姓名、城市，以及 paid 订单数。没有订单的用户也要出现，订单数为 0。\n\n### 验收\n\n- [ ] 能不看笔记复述本节目标\n- [ ] 示例可以自己重做一遍\n- [ ] 能举出一个易错并改对\n- [ ] 能说出本节不该用的场景\n\n### 导航\n\n- 上级：`多表操作`\n- 上一节：（本节起）\n- 下一节：子查询\n","children":[]},{"id":"SQL.多表操作.子查询","title":"子查询","level":"??","content":"### 课前\n\n- **目标**：能区分标量子查询、IN 列表与派生表，并知道何时改写成 JOIN。\n- **先修**：先看上一节；样例与全平台一致。\n- **样例**：统一样例：users / orders / order_items。\n\n### 是什么\n\n子查询是嵌在查询里的查询。标量子查询必须只返回一行一列。IN 适合小集合；大表关联优先 JOIN。派生表要有别名。\n\n### 怎么做\n\n```sql\nSELECT user_id, user_name\nFROM users\nWHERE user_id IN (\n  SELECT user_id FROM orders WHERE status = 'paid'\n);\n\nSELECT u.user_name, t.gmv\nFROM users AS u\nJOIN (\n  SELECT user_id, SUM(amount) AS gmv\n  FROM orders\n  WHERE status = 'paid'\n  GROUP BY user_id\n) AS t ON t.user_id = u.user_id;\n```\n\n### 用在哪\n\n1. 找出至少有一笔支付的用户。\n2. 把「用户 GMV」做成派生表再补姓名。\n3. 用标量子查询取全表最大金额做对比。\n\n### 易错对照\n\n| 错法 | 纠正 |\n|---|---|\n| 标量子查询返回多行 | 加聚合或 LIMIT 1，或改 JOIN |\n| 相关子查询在大表上逐行执行 | 改成先 GROUP BY 再 JOIN |\n| 派生表忘记别名 | FROM ( ... ) AS t |\n\n### 动手\n\n查询 GMV 高于「全体 paid 订单平均金额」的订单号与金额。\n\n### 验收\n\n- [ ] 能不看笔记复述本节目标\n- [ ] 示例可以自己重做一遍\n- [ ] 能举出一个易错并改对\n- [ ] 能说出本节不该用的场景\n\n### 导航\n\n- 上级：`多表操作`\n- 上一节：JOIN\n- 下一节：UNION\n","children":[]},{"id":"SQL.多表操作.UNION","title":"UNION","level":"??","content":"### 课前\n\n- **目标**：能用 UNION / UNION ALL 叠结果，并说清何时不能去重。\n- **先修**：先看上一节；样例与全平台一致。\n- **样例**：统一样例：users / orders / order_items。\n\n### 是什么\n\nUNION 上下拼接列数、类型必须兼容，按位置对齐而不是按列名。UNION 会去重（多一次排序）；UNION ALL 保留重复，日志拼接应优先 ALL。\n\n### 怎么做\n\n```sql\nSELECT order_id, 'paid' AS src, amount\nFROM orders\nWHERE status = 'paid'\nUNION ALL\nSELECT order_id, 'item' AS src, price * qty\nFROM order_items;\n```\n\n### 用在哪\n\n1. 把两个渠道的订单叠成一张投放表。\n2. 事件流与订单流按同一列结构拼接再统一清洗。\n3. 明确要去重时才用 UNION。\n\n### 易错对照\n\n| 错法 | 纠正 |\n|---|---|\n| 列顺序不一致导致金额和 ID 对调 | 两边 SELECT 列顺序、含义逐列对齐 |\n| 该保留重复却用了 UNION | 日志用 UNION ALL |\n| 试图用 UNION 代替 JOIN | 左右补列用 JOIN，上下叠行用 UNION |\n\n### 动手\n\n把 status='paid' 与 status='cancelled' 的订单叠在一起，多加一列 bucket 标记来源，保留重复行。\n\n### 验收\n\n- [ ] 能不看笔记复述本节目标\n- [ ] 示例可以自己重做一遍\n- [ ] 能举出一个易错并改对\n- [ ] 能说出本节不该用的场景\n\n### 导航\n\n- 上级：`多表操作`\n- 上一节：子查询\n- 下一节：（本节止）\n","children":[]}],"lessonParent":true},{"id":"SQL.聚合分析","title":"聚合分析","level":"??","content":"### 课前 · 章节导读\n\n- **章节**：聚合分析\n- **为什么学**：把「聚合分析」拆成可练习的小节，避免只记目录。\n- **学完能做什么**：按顺序完成下面每一节的「动手」，并能讲清本节边界。\n- **纪律**：使用全平台统一样例（订单 / 用户 / Superstore），不要每节换一套数据。\n\n### 本节地图\n\n| # | 节点 | 难度 | 一句话 |\n|---|---|---|---|\n| 1 | GROUP BY | ?? | 目标：能按维度聚合，并遵守「SELECT 的非聚合列必须出现在 GROUP BY」。 |\n| 2 | HAVING | ?? | 目标：能在分组之后过滤组，而不是把条件和 WHERE 混在一起。 |\n| 3 | 窗口函数 | ?? | 目标：能写 OVER(PARTITION BY … ORDER BY …)，区分… |\n\n### 推荐顺序\n\n```text\nGROUP BY → HAVING → 窗口函数\n```\n\n### 怎么学\n\n1. 先看地图，知道有哪些节点。\n2. 按顺序打开，每节做完「动手」再往下。\n3. 用自己的表名替换样例，做一次小样本迁移。\n\n### 验收\n\n| 检查 | 标准 |\n|---|---|\n| 主路径 | 每节示例能重做 |\n| 易错 | 至少能举出 2 个反例 |\n| 口述 | 不看笔记讲清「聚合分析」解决什么 |\n\n### 下一动\n\n从第 1 个节点开始。本章共 **3** 个直接下级。\n","children":[{"id":"SQL.聚合分析.GROUP BY","title":"GROUP BY","level":"??","content":"### 课前\n\n- **目标**：能按维度聚合，并遵守「SELECT 的非聚合列必须出现在 GROUP BY」。\n- **先修**：先看上一节；样例与全平台一致。\n- **样例**：统一样例：users / orders / order_items。\n\n### 是什么\n\nGROUP BY 把行折成组，组内用 COUNT/SUM/AVG。选了 user_name 却只按 user_id 分组，在严格模式下会报错；宽松模式下会随机挑一个名字。\n\n### 怎么做\n\n```sql\nSELECT\n  user_id,\n  COUNT(*) AS order_cnt,\n  SUM(CASE WHEN status = 'paid' THEN amount ELSE 0 END) AS gmv\nFROM orders\nGROUP BY user_id;\n```\n\n### 用在哪\n\n1. 每个用户的支付 GMV。\n2. 每个城市的用户数。\n3. 用条件聚合一次算出支付额和取消额。\n\n### 易错对照\n\n| 错法 | 纠正 |\n|---|---|\n| SUM 前没过滤或没 CASE，把取消单算进 GMV | SUM(CASE WHEN status='paid' THEN amount END) |\n| GROUP BY 漏了维度，人数被并错 | 分组键与 SELECT 维度一致 |\n| COUNT(*) 与 COUNT(amount) 混用 | COUNT(*) 计行，COUNT(列) 忽略 NULL |\n\n### 动手\n\n按 user_id 统计 paid 订单数与 GMV，GMV 为空时显示 0。\n\n### 验收\n\n- [ ] 能不看笔记复述本节目标\n- [ ] 示例可以自己重做一遍\n- [ ] 能举出一个易错并改对\n- [ ] 能说出本节不该用的场景\n\n### 导航\n\n- 上级：`聚合分析`\n- 上一节：（本节起）\n- 下一节：HAVING\n","children":[]},{"id":"SQL.聚合分析.HAVING","title":"HAVING","level":"??","content":"### 课前\n\n- **目标**：能在分组之后过滤组，而不是把条件和 WHERE 混在一起。\n- **先修**：先看上一节；样例与全平台一致。\n- **样例**：统一样例：users / orders / order_items。\n\n### 是什么\n\nWHERE 过滤行，HAVING 过滤组。`HAVING SUM(amount) >= 100` 不能写进 WHERE。能在 WHERE 做的过滤不要推到 HAVING，否则会先把不该参与的行算进聚合。\n\n### 怎么做\n\n```sql\nSELECT user_id, SUM(amount) AS gmv\nFROM orders\nWHERE status = 'paid'\nGROUP BY user_id\nHAVING SUM(amount) >= 200;\n```\n\n### 用在哪\n\n1. 只要 GMV 达到门槛的用户。\n2. 找出订单数大于 1 的复购用户。\n3. 和 WHERE 配合：先丢掉未支付，再按汇总门槛筛人。\n\n### 易错对照\n\n| 错法 | 纠正 |\n|---|---|\n| 把 HAVING 条件写进 WHERE | 聚合条件只能 HAVING |\n| 只写 HAVING 不过滤 status | 行级条件放 WHERE，减少进组的行 |\n| HAVING 里用了没聚合也没分组的列 | 要么进 GROUP BY，要么套聚合 |\n\n### 动手\n\n找出 paid GMV ≥ 150 的 user_id，并给出 GMV。\n\n### 验收\n\n- [ ] 能不看笔记复述本节目标\n- [ ] 示例可以自己重做一遍\n- [ ] 能举出一个易错并改对\n- [ ] 能说出本节不该用的场景\n\n### 导航\n\n- 上级：`聚合分析`\n- 上一节：GROUP BY\n- 下一节：窗口函数\n","children":[]},{"id":"SQL.聚合分析.窗口函数","title":"窗口函数","level":"??","content":"### 课前\n\n- **目标**：能写 OVER(PARTITION BY … ORDER BY …)，区分它和 GROUP BY。\n- **先修**：先看上一节；样例与全平台一致。\n- **样例**：统一样例：users / orders / order_items。\n\n### 是什么\n\n窗口函数在不折叠行的前提下做排名、累计、前后值。`ROW_NUMBER` 每个分区从 1 编号；`RANK` 会并列跳号。累计金额用 `SUM() OVER (ORDER BY …)`。\n\n### 怎么做\n\n```sql\nSELECT\n  order_id,\n  user_id,\n  amount,\n  ROW_NUMBER() OVER (PARTITION BY user_id ORDER BY created_at, order_id) AS rn,\n  SUM(amount) OVER (\n    PARTITION BY user_id ORDER BY created_at, order_id\n  ) AS running_gmv\nFROM orders\nWHERE status = 'paid';\n```\n\n### 用在哪\n\n1. 每个用户按时间的第 N 笔订单。\n2. 用户内累计 GMV。\n3. 取每个用户最近一笔（rn = 1 且时间倒序）。\n\n### 易错对照\n\n| 错法 | 纠正 |\n|---|---|\n| 窗口写成 GROUP BY，明细行没了 | 要保留明细就用 OVER，不要分组 |\n| ORDER BY 不稳定导致 rn 抖动 | 排序键加上主键 |\n| 把窗口结果又和原表错误 JOIN 翻倍 | 窗口直接写在同一层 SELECT |\n\n### 动手\n\n给每个用户的 paid 订单按时间编号，并计算该用户截至该单的累计金额。\n\n### 验收\n\n- [ ] 能不看笔记复述本节目标\n- [ ] 示例可以自己重做一遍\n- [ ] 能举出一个易错并改对\n- [ ] 能说出本节不该用的场景\n\n### 导航\n\n- 上级：`聚合分析`\n- 上一节：HAVING\n- 下一节：（本节止）\n","children":[]}],"lessonParent":true},{"id":"SQL.数据定义","title":"数据定义","level":"???","content":"### 课前\n\n- **目标**：能用 CREATE TABLE 声明类型、主键和非空，并知道 DDL 是结构而不是查询。\n- **先修**：先看上一节；样例与全平台一致。\n- **样例**：统一样例：users / orders / order_items。\n\n### 是什么\n\n数据定义（DDL）描述表、列、约束。类型决定能不能排序和聚合；主键保证一行一个订单。分析库里建表前先写清粒度：一行是什么。\n\n### 怎么做\n\n```sql\nCREATE TABLE orders (\n  order_id    BIGINT PRIMARY KEY,\n  user_id     BIGINT NOT NULL,\n  amount      DECIMAL(12, 2),\n  status      VARCHAR(16) NOT NULL,\n  created_at  TIMESTAMP NOT NULL\n);\n```\n\n### 用在哪\n\n1. 为教程样例建订单表。\n2. 给状态、时间加非空，避免脏数据进仓。\n3. 和 BI 同学对齐：金额用 DECIMAL 而不是浮点。\n\n### 易错对照\n\n| 错法 | 纠正 |\n|---|---|\n| 金额用 FLOAT | 用 DECIMAL，避免 0.1 二进制误差 |\n| 没有主键，重复订单进表 | 业务主键或代理键必须声明 |\n| 先插数据再改类型 | 先定契约再灌数 |\n\n### 动手\n\n写出 users 表的 CREATE TABLE：user_id 主键，user_name 非空，city 可空，created_at 非空。\n\n### 验收\n\n- [ ] 能不看笔记复述本节目标\n- [ ] 示例可以自己重做一遍\n- [ ] 能举出一个易错并改对\n- [ ] 能说出本节不该用的场景\n\n### 导航\n\n- 上级：`SQL`\n- 上一节：聚合分析\n- 下一节：性能优化\n","children":[]},{"id":"SQL.性能优化","title":"性能优化","level":"???","content":"### 课前\n\n- **目标**：能用过滤、列裁剪和索引思路判断一条查询为什么慢。\n- **先修**：先看上一节；样例与全平台一致。\n- **样例**：统一样例：users / orders / order_items。\n\n### 是什么\n\n慢查询先看：是不是 `SELECT *`、是不是没过滤、是不是 JOIN 前没聚合、是不是在列上套函数导致索引用不上。先把口径写对，再谈索引。EXPLAIN 用来看访问方式，而不是背一个「最优计划」。\n\n### 怎么做\n\n```sql\nEXPLAIN\nSELECT order_id, amount\nFROM orders\nWHERE status = 'paid'\n  AND created_at >= DATE '2024-01-01';\n```\n可考虑索引 `(status, created_at)`，让过滤和范围扫描走在一起。\n\n### 用在哪\n\n1. 日报只扫支付订单，不要全表。\n2. 大明细先按订单聚合再关联。\n3. 深分页改成按主键续页。\n\n### 易错对照\n\n| 错法 | 纠正 |\n|---|---|\n| 先加一堆索引再看 SQL | 先改写查询，确认过滤列 |\n| 在 WHERE 里对列做函数 | 把函数挪到常量一侧，或建表达式索引 |\n| SELECT * 拉回几十列再在 BI 里丢 | SQL 里就只取需要的列 |\n\n### 动手\n\n把「每个用户最近一笔 paid 订单」写成先过滤、再窗口或分组的查询，并说明哪一列适合做索引。\n\n### 验收\n\n- [ ] 能不看笔记复述本节目标\n- [ ] 示例可以自己重做一遍\n- [ ] 能举出一个易错并改对\n- [ ] 能说出本节不该用的场景\n\n### 导航\n\n- 上级：`SQL`\n- 上一节：数据定义\n- 下一节：事务与安全\n","children":[]},{"id":"SQL.事务与安全","title":"事务与安全","level":"???","content":"### 课前\n\n- **目标**：能说明事务的提交/回滚，以及分析账号为什么不能要写权限。\n- **先修**：先看上一节；样例与全平台一致。\n- **样例**：统一样例：users / orders / order_items。\n\n### 是什么\n\n事务把多步改动变成全成或全不成。分析查询通常只读，不要包在长事务里锁表。账号按最小权限：报表用户只有 SELECT。拼接 SQL 字符串会注入，条件必须用参数。\n\n### 怎么做\n\n```sql\nBEGIN;\nUPDATE orders SET status = 'paid' WHERE order_id = 101 AND status = 'created';\nINSERT INTO order_events(order_id, event_type) VALUES (101, 'pay');\nCOMMIT;\n-- 出错则 ROLLBACK;\n```\n\n### 用在哪\n\n1. 支付状态与事件日志必须一起成功。\n2. BI 连接使用只读账号。\n3. 应用层用参数化查询，不把用户输入拼进 SQL。\n\n### 易错对照\n\n| 错法 | 纠正 |\n|---|---|\n| 报表连接用了管理员账号 | 单独只读账号 |\n| 长事务里跑大查询 | 只读短查询，写事务尽量短 |\n| 字符串拼接 WHERE 条件 | 使用绑定参数 |\n\n### 动手\n\n写出一段事务：把订单 102 标为 cancelled，并插入一条 cancel 事件；任一步失败则回滚。说明报表账号至少不该有哪类权限。\n\n### 验收\n\n- [ ] 能不看笔记复述本节目标\n- [ ] 示例可以自己重做一遍\n- [ ] 能举出一个易错并改对\n- [ ] 能说出本节不该用的场景\n\n### 导航\n\n- 上级：`SQL`\n- 上一节：性能优化\n- 下一节：（本节止）\n","children":[]}],"source":"tutorials_v2_graph"}